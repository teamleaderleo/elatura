name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - name: Archive checked-out source for exact local review
        run: git archive --format=zip --output=elatura-source.zip HEAD
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: elatura-source-bootstrap
          path: elatura-source.zip
          if-no-files-found: error
          retention-days: 1
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 22
          cache: npm
      - name: Install frozen dependencies
        run: npm ci --ignore-scripts
      - run: npm run check
      - name: Run synthetic materialization benchmark smoke
        run: >-
          node --expose-gc scripts/benchmark-synthetic-materialization.mjs
          --turns 100
          --branches-every 5
          --payload-bytes 64
          --max-groups 4
          --iterations 1
